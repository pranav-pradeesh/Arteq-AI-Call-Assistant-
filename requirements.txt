# Arteq Hospital Voice Agent — pinned dependencies.
#
# Every version is EXACT (==) so every machine installs the identical stack.
# Loose ranges are what let pydantic drift to 2.7.x and break ChatContext
# serialization; never reintroduce them. To upgrade: bump here, test, commit.

# ── Web framework ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
websockets==14.2
python-multipart==0.0.9
jinja2==3.1.4

# ── Database ───────────────────────────────────────────────────────────────────
asyncpg==0.29.0

# ── HTTP client (HIS adapters + SMS + Plivo) ───────────────────────────────────
httpx==0.28.1
aiohttp==3.14.0

# ── AI / LLM ──────────────────────────────────────────────────────────────────
groq==1.4.0

# ── Data validation ────────────────────────────────────────────────────────────
# pydantic-core < 2.33 mis-serializes plain-str ChatContext content (Instructions
# is a str subclass and hijacks the union serializer → 'str' has no attribute
# 'audio'). Keep both pinned together.
pydantic==2.11.9
pydantic-core==2.33.2
pydantic-settings==2.6.1

# ── Auth (dashboard JWT) ───────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0

# ── Observability ──────────────────────────────────────────────────────────────
structlog==24.2.0
prometheus-client==0.25.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
tenacity==8.3.0
pytz==2024.1
numpy==2.2.6

# ── LiveKit Voice Agent ────────────────────────────────────────────────────────
# agents + plugins version independently; this exact set is verified compatible.
# Run: pip install -r requirements.txt
livekit==1.1.8
livekit-api==1.1.0
livekit-agents==1.5.16
livekit-plugins-openai==1.1.7
livekit-plugins-sarvam==1.1.7
livekit-plugins-silero==1.1.7
livekit-plugins-noise-cancellation==0.2.5

# ── Dev / test ─────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
ruff==0.15.15
bcrypt==4.2.1
email-validator==2.2.0
redis==5.0.8
